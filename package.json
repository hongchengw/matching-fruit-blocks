{
  "name": "matching-fruit-blocks",
  "version": "0.1.0",
  "description": "Farmer's Match: a pixel-art memory card game",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "e2e": "playwright test",
    "serve": "node scripts/serve.js"
  },
  "devDependencies": {
    "@playwright/test": "^1.49.0",
    "jsdom": "^25.0.1",
    "vitest": "^2.1.8"
  }
}
